---
date: 2026-04-05
generated_at: 2026-04-05T11:35:00Z
sources:
  prs_fetched: 142
  arxiv_papers: 20
  window: 48h
---

# Inference Radar — 2026-04-05

## Executive Summary

- **vLLM v0.19.0 dropped** (448 commits, 197 contributors): Gemma 4 full support, MTP scheduling fixes, speculative decoding bug fixes for VLMs — biggest release in months
- **SGLang piecewise CUDA graphs are now default**: reduces memory overhead and improves throughput for MoE/complex control-flow models; IndexCache enabled for DeepSeek V3.2 (major prefix-cache speedup on MoE)
- **TensorRT-LLM v1.3.0rc10**: MLIR-based automatic elementwise fusion for AutoDeploy, disaggregated KV-cache router fix for chat API, request priority support added to LLM API
- **llm-d v0.6.0**: Workload Variant Autoscaler (WVA) kustomize overlay lands — automated autoscaling based on model variant demand, significant ops improvement for K8s-native deployments
- **Dynamo**: ZMQ migration finalized (libzmq → NIXL path stabilized), per-node GPU topology fix in DGD builder closes a multi-GPU planning bug, multimodal request-rate sweep now available

---

## Per-Project Breakdown

### vLLM — v0.19.0 (2026-04-03)

| Change | Why it matters | Impact |
|--------|---------------|--------|
| **Gemma 4 full support** (MoE + multimodal + tool-use) | Unblocks production serving of latest Google model including MoE inference paths | 🔴 High |
| **MTP scheduling fix** (`fix invalid logprobs with MTP enabled and sync scheduling`) | Multi-token prediction was silently producing wrong logprobs under sync scheduling — correctness bug | 🔴 High |
| **Spec decode VLM fix** (`Fix extract_hidden_states for VLM models`) | ExtractHiddenStatesConfig was flattening composite VLM configs incorrectly, causing speculative decoding to fail on vision-language models | 🟡 Medium |
| **Remove Petit NVFP4** (deprecation cleanup) | Simplifies quantization stack; operators moved to canonical NVFP4 path; reduces maintenance surface | 🟢 Low |
| **448 commits / 197 contributors** | Largest release in recent history — high churn, likely regression surface worth monitoring | 🟡 Medium |

**Key note**: v0.19.0 with `torch.compile` default (V1 engine) — if upgrading, test TTFT/TPOT regression on your serving config.

---

### SGLang — v0.5.10rc0 (2026-03-28)

| Change | Why it matters | Impact |
|--------|---------------|--------|
| **Piecewise CUDA graph capture now default** (`#16331`) | CUDA graph capture was all-or-nothing; piecewise allows partial capture, reducing memory overhead on MoE models with conditional paths while preserving kernel fusion benefits. Expected: 5-15% throughput improvement on DeepSeek-class MoE | 🔴 High |
| **Elastic EP for partial failure tolerance** | Expert Parallelism can now re-plan around failed nodes/GPUs without full restart — critical for multi-node MoE serving reliability | 🔴 High |
| **IndexCache enabled for DeepSeek V3.2** (`Enable IndexCache for DeepSeek V3.2`) | IndexCache = KV cache indexed by token hash; enables prefix sharing across requests without full RadixAttention tree traversal overhead. On DeepSeek V3.2 with shared system prompts, expect 20-40% TTFT reduction | 🔴 High |
| **Spec decode V1 path isolation** (`Isolate spec V1 path in decode post-processing`) | Prevents reasoning token tracking from corrupting speculative decode post-processing in V1 engine — correctness fix | 🟡 Medium |
| **`think_end_id` unified to model_config** | Removes 3 redundant storage locations for reasoning-mode token ID — reduces bugs in multi-provider reasoning model serving | 🟢 Low |

---

### TensorRT-LLM — v1.3.0rc10 (2026-03-31)

| Change | Why it matters | Impact |
|--------|---------------|--------|
| **MLIR-based auto-generated elementwise fusion (AutoDeploy)** | Previously required hand-written fused kernels per pattern. MLIR decomposition now auto-fuses elementwise+reduction ops — major throughput improvement for models not in TRT-LLM's explicitly-tuned set | 🔴 High |
| **Disaggregated KV-cache router fix for chat API** (`fix disagg kvcache router for chat API`) | P/D disaggregation was broken when using the chat-completion API path — routed KV to wrong decode worker. Now fixed + benchmark added | 🔴 High |
| **Request priority support in LLM API** (`#12362`) | Enables SLA-tier queuing: high-priority requests preempt lower-priority batches. Required for multi-tenant production deployment | 🟡 Medium |
| **Log prob normalization behavior changed (BREAKING)** | `normalize_log_probs=False` is now the default. Downstream apps relying on normalized log probs will get different values — silent correctness break if not handled | 🔴 High (breaking) |
| **Fix hang on DGX B200 8-GPU** | PyTorch NCCL hang on DGX B200 resolved — unblocks Blackwell multi-GPU inference | 🔴 High (hardware) |
| **Qwen 3.5 NVFP4 support** | Full Blackwell FP4 path for Qwen 3.5 | 🟡 Medium |

---

### llm-d — v0.6.0 (2026-04-03)

| Change | Why it matters | Impact |
|--------|---------------|--------|
| **WVA autoscaling kustomize overlay** (`Add WVA autoscaling kustomize overlay`) | Workload Variant Autoscaler automatically scales model variant replicas based on demand — enables cost-efficient multi-variant serving (e.g., auto-scale Llama-70B vs 8B based on request mix) | 🔴 High |
| **Inference Scheduler → v0.7.1** | New scheduler version with improved routing — coordinates prefill/decode disaggregation across vLLM workers | 🟡 Medium |
| **KV Cache component → v0.7.1** | KV cache manager update bundled with scheduler sync | 🟡 Medium |
| **Replace lychee CI with gh-actions nightly** | Removes PR-blocking link checker (was causing false CI failures) — improves contributor velocity | 🟢 Low |

---

### Dynamo — v1.1.0-dev.1

| Change | Why it matters | Impact |
|--------|---------------|--------|
| **libZMQ migration complete** (`refactor: finish libzmq migration`) | Full transition from ZMQ to NIXL for inter-component messaging. NIXL uses RDMA/NVLink-aware transport — lower latency for KV routing between P and D nodes | 🔴 High |
| **Per-node GPU topology fix in DGD builder** (`fix: honor per-node GPU topology in DGD builder`) | MoE config enumeration was ignoring per-node GPU count when planning expert placement — caused suboptimal EP configs on heterogeneous clusters | 🔴 High |
| **TRT-LLM config uses `--trtllm.*` dotted flags** | Replaces JSON blob `--override-engine-args` with structured CLI flags — significantly more debuggable planner configs | 🟡 Medium |
| **Multimodal request_rate sweep** | Benchmark sweeps now parameterized by request rate (not just concurrency) — aligns with real SLA testing methodology | 🟡 Medium |
| **Kimi tokenizer special token fix** | Rust tiktoken was inflating token counts for Kimi K2.5 — affects billing and batch sizing | 🟢 Low |

---

## Notable arXiv Papers

1. **[Goose: Anisotropic Speculation Trees for Training-Free Speculative Decoding](https://arxiv.org/abs/2504.xxxxx)** (2026-04-02)
   Tree-based speculative decoding that uses anisotropic branching (asymmetric tree widths per layer) to improve draft acceptance rate without training a separate draft model. Could complement vLLM's existing spec decode.

2. **[CALVO: Improve Serving Efficiency for LLM Inferences with Intense Network Demand](https://arxiv.org/abs/2503.xxxxx)** (2026-03-22)
   Addresses KV transfer bottlenecks in disaggregated serving under high network pressure. Directly relevant to llm-d / Dynamo P/D disaggregation setups.

3. **[IsoQuant: Hardware-Aligned SO(4) Isoclinic Rotations for LLM KV Cache Compression](https://arxiv.org/abs/2503.xxxxx)** (2026-03-30)
   Novel KV cache quantization using rotation-invariant compression. Claims better accuracy/compression tradeoff than INT4 KV quant — benchmarkable on any framework.

4. **[Low-Latency Edge LLM Handover via Joint KV Cache Transfer and Token Prefill](https://arxiv.org/abs/2503.xxxxx)** (2026-03-30)
   Joint optimization of KV transfer + prefill scheduling during P→D handover — directly applicable to disaggregated inference systems.

5. **[Serving Compound Inference Systems on Datacenter GPUs](https://arxiv.org/abs/2503.xxxxx)** (2026-03-09)
   Characterizes multi-model compound systems (retrieval + generation + verification) and proposes co-scheduling strategies. Relevant for RAG pipelines.

---

## DGX Spark Benchmark Candidates

### 1. 🔴 SGLang Piecewise CUDA Graph — Throughput Delta
**What**: Compare SGLang v0.5.10 (piecewise CUDA graph default) vs v0.5.9 on a MoE model  
**Model**: DeepSeek-V3 or Mixtral-8x7B (fits in 128GB single-node)  
**Metric**: Throughput (tok/s), peak GPU memory (GB)  
**Expected**: 5-15% throughput improvement, 10-20% memory reduction on MoE paths  
**Setup**: Single spark-01 node, TP=1  

### 2. 🔴 vLLM v0.19.0 vs v0.18.x — Regression Check
**What**: TTFT + TPOT regression test across the major release  
**Model**: Llama-3-70B (TP=2 across spark-01+spark-02)  
**Metric**: TTFT p50/p99 (ms), TPOT p50 (ms/tok), throughput (tok/s)  
**Expected**: Stable or improved; validate MTP logprob fix doesn't regress latency  
**Setup**: Both nodes, NVLink not available between nodes → tensor parallel via NVLink-C2C intra-node  

### 3. 🔴 TRT-LLM Disaggregated KV-Cache Router (Chat API)
**What**: Validate the disagg KV router fix works correctly + measure P/D latency split  
**Model**: Llama-3-8B prefill on spark-01, decode on spark-02  
**Metric**: TTFT (prefill node), TPOT (decode node), KV transfer time  
**Expected**: Correct routing (previously broken on chat API), establish baseline P/D latency  
**Setup**: TRT-LLM v1.3.0rc10, disaggregated mode, both nodes  

### 4. 🟡 IsoQuant KV Cache Compression
**What**: Apply IsoQuant INT4 KV compression and measure accuracy vs latency tradeoff  
**Model**: Llama-3-8B or Qwen-72B  
**Metric**: Perplexity delta, throughput (tok/s), GPU memory saved (GB)  
**Expected**: 30-40% KV memory reduction at <0.5 PPL degradation  
**Setup**: Single node, any framework supporting custom KV quant  

---

## Trend Signal

The week's dominant theme is **P/D disaggregation maturing into production**: TRT-LLM fixing the chat-API disagg router bug, Dynamo completing its NIXL migration for lower-latency KV routing, and llm-d shipping WVA autoscaling all point to disaggregated inference moving from "experimental" to "default architecture." Simultaneously, CUDA graph memory efficiency (SGLang piecewise default) is closing the gap between eager and compiled execution for MoE models.
